#include "uart_module.h"

void uart_init(void) {
    // Initialize UART peripheral
}

void uart_send_command(char command) {
    // Send command to Arduino via UART
    // For demonstration, implement UART transmission here
}
